import { put } from "@vercel/blob";
import { defaultMedia,getMediaSettings,type MediaSettings } from "../../../../lib/media";
const slots:(keyof MediaSettings)[]=["heroVideo","duetVideo","heroImage","foodImage"];
function authorised(request:Request){const configured=process.env.ADMIN_PASSWORD;return Boolean(configured&&request.headers.get("x-admin-password")===configured)}
export async function GET(request:Request){if(!authorised(request))return Response.json({error:"Incorrect admin password"},{status:401});return Response.json({settings:await getMediaSettings(),storageReady:Boolean(process.env.BLOB_READ_WRITE_TOKEN)})}
export async function PUT(request:Request){if(!authorised(request))return Response.json({error:"Incorrect admin password"},{status:401});if(!process.env.BLOB_READ_WRITE_TOKEN)return Response.json({error:"Connect a Vercel Blob store first"},{status:503});const input=await request.json() as Partial<MediaSettings>;const settings={...defaultMedia};for(const slot of slots){const value=String(input[slot]??"").trim();if(!value||value.length>2000||(!value.startsWith("/")&&!/^https:\/\//i.test(value)))return Response.json({error:`Invalid ${slot} URL`},{status:400});settings[slot]=value}await put("site-config/media-settings.json",JSON.stringify(settings),{access:"public",contentType:"application/json",addRandomSuffix:false,allowOverwrite:true,cacheControlMaxAge:60});return Response.json({settings})}
